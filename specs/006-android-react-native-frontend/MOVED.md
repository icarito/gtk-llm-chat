# Moved

This active change is now owned by the sibling repository
`../gtk-llm-chat-android` at
`openspec/changes/android-react-native-frontend/`. These original files remain
only as historical context; do not apply new Android work from this directory.

